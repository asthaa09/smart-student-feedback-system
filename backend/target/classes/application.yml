spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_feedback
    username: root
    password: Astha@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwt:
    secret: "change-this-to-a-very-long-random-secret-key-for-production"
    expiration-ms: 86400000